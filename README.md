# Voice Agent - 桌面语音助手

一个跨平台的桌面语音助手，通过全局热键唤醒，支持语音听写和智能指令执行。

## 功能特性

- 🎙️ **语音听写** - 长按热键直接输入语音转文字
- 🤖 **智能指令** - 单击热键执行语音指令
- 🔧 **工具执行** - 支持打开应用、执行命令、文件操作等
- 🔒 **权限控制** - 危险操作需要确认

## 架构

```
┌─────────────────────────────────────┐
│     Electron Desktop Client          │
│  (UI + 热键 + 音频采集 + 工具执行)     │
└─────────────────┬───────────────────┘
                  │ HTTP
                  ▼
┌─────────────────────────────────────┐
│      Backend API (Docker)            │
│  ┌─────────┐ ┌─────────┐ ┌────────┐ │
│  │   STT   │ │   LLM   │ │ Intent │ │
│  └─────────┘ └─────────┘ └────────┘ │
└─────────────────────────────────────┘
```

## 快速开始

### 1. 启动后端服务

```bash
cd backend

# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
# 编辑 .env 填入 API Key

# 开发模式
npm run dev

# 或使用 Docker
cd ..
docker-compose up -d
```

### 2. 启动客户端

```bash
# 在项目根目录
npm install
npm run dev
```

## 热键操作

| 操作 | 模式 | 说明 |
|------|------|------|
| 长按 | 听写模式 | 语音直接转为文字输入 |
| 单击 | 指令模式 | 识别意图并执行操作 |
| 双击 | Agent 模式 | 强制进入 LLM 推理 |

默认热键：`Ctrl+Space` (Windows) / `Cmd+Space` (macOS)

## 环境变量

```bash
# 后端配置
OPENAI_API_KEY=sk-xxx        # Whisper STT
ANTHROPIC_API_KEY=sk-ant-xxx # Claude LLM
LLM_PROVIDER=claude          # claude | openai
```

## 目录结构

```
voice-agent/
├── backend/                 # 后端 API 服务
│   ├── src/
│   │   ├── routes/         # API 路由
│   │   ├── services/       # 业务逻辑
│   │   │   ├── stt/       # 语音转文字
│   │   │   ├── llm/       # 大语言模型
│   │   │   └── intent/    # 意图识别
│   │   └── utils/         # 工具函数
│   └── Dockerfile
├── src/                    # Electron 客户端
│   ├── main/              # 主进程
│   │   ├── hotkey.ts     # 热键管理
│   │   ├── tray.ts       # 系统托盘
│   │   └── services/     # 工具执行
│   ├── renderer/          # React 渲染进程
│   │   ├── components/   # UI 组件
│   │   ├── hooks/        # React Hooks
│   │   └── store/        # 状态管理
│   └── preload/          # 预加载脚本
└── docker-compose.yml
```

## 技术栈

- **桌面框架**: Electron
- **前端**: React + TypeScript + TailwindCSS
- **后端**: Node.js + Express
- **STT**: OpenAI Whisper API
- **LLM**: Claude / OpenAI (可切换)
- **状态管理**: Zustand

## 开发

```bash
# 类型检查
npm run typecheck

# 构建
npm run build

# 打包
npm run package
```

## License

MIT

